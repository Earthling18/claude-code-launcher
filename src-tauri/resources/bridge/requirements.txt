# Web 框架
fastapi>=0.109.0
uvicorn[standard]==0.27.0

# 数据验证
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Claude Agent SDK
# 注意：0.1.25+ 版本改进了错误处理，支持将 "Prompt is too long" 等错误传递给 Claude 重试
claude-agent-sdk>=0.1.25

# 异步支持
anyio>=4.0.0

# HTTP 客户端（COS API 调用）
httpx>=0.27.0

# YAML 解析（Skill frontmatter）
PyYAML>=6.0.0

# MCP 服务器
mcp>=1.0.0

# 定时任务
apscheduler>=3.10.0

# 异步 SQLite（数据看板）
aiosqlite>=0.19.0

# 飞书/Lark API
lark-oapi>=1.0.0

# 进程管理
psutil>=5.9.0

# Bridge Client (bridge_clientv3.py)
aiohttp>=3.9.0
websockets>=12.0

# 其他
python-dotenv>=1.0.0
